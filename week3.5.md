
### שאלה 1: מילונים ומניית תדירויות (Frequency Map)

בחברת הייטק ישנה מערכת המתעדת את כניסת העובדים לבניין. כל אימת שעובד מעביר כרטיס, מספר הזהות שלו (מספר שלם) נרשם.

כתוב פונקציה בשם `find_most_frequent_visitor(log_list)` המקבלת רשימה של מספרי זהות המייצגת את יומן הכניסות.

על הפונקציה למצוא ולהחזיר את מספר הזהות של העובד שנכנס הכי הרבה פעמים לבניין.

**דגשים ודרישות חובה (ברמת מבחן מה"ט):**

- **יעילות זמן ריצה:** חובה לפתור את השאלה בסיבוכיות זמן של $O(N)$ (כאשר $N$ הוא אורך הרשימה).
    
- **איסור "קסמים":** חל איסור מוחלט להשתמש במתודות מובנות כגון `.count()` על רשימות, ואין להשתמש בפונקציה המובנית `max()`. עליך לבנות את מילון התדירויות ולמצוא את המקסימום בעצמך.
    
- **מקרי קצה:** במידה והרשימה ריקה, על הפונקציה להחזיר `-1`.
    
- שים לב: אם יש מספר עובדים בעלי אותה כמות מקסימלית של כניסות, מותר להחזיר את כל אחד מהם.
    

---

### שאלה 2: קבוצות (Sets) ובדיקת קיום ב-$O(1)$

במכללה התקיימו שתי סדנאות שונות. כל סדנה הפיקה רשימה של תעודות הזהות (מחרוזות) של הסטודנטים שהשתתפו בה.

כתוב פונקציה בשם `count_common_students(workshop_a, workshop_b)` המקבלת שתי רשימות של מחרוזות. על הפונקציה להחזיר את **מספר** הסטודנטים שהשתתפו _בשתי_ הסדנאות גם יחד.

**דגשים ודרישות חובה:**

- **יעילות זמן ריצה:** פתרון של לולאה בתוך לולאה ($O(N \cdot M)$) יגרור פסילה של רוב הניקוד על הסעיף! חובה לפתור בסיבוכיות של $O(N + M)$ באמצעות שימוש ב-Set.
    
- **פייתון אקדמי:** אסור להשתמש באופרטור החיתוך המובנה של קבוצות (כגון `set1 & set2` או `set1.intersection(set2)`). עליך לבצע את המעבר והבדיקה ידנית בעזרת לולאה ובדיקת קיום (`in`).
    
- **מקרי קצה:** יש להתייחס למצב שבו אחת הרשימות (או שתיהן) ריקות (התוצאה צריכה להיות 0). הרשימות עלולות לכלול כפילויות (סטודנט שהעביר כרטיס פעמיים באותה סדנה), אך סטודנט כזה ייספר רק פעם אחת כמשותף.

---

### שאלה 3: קריאת קבצים (File I/O) ושילוב טאפלים ומילונים

נתון קובץ טקסט בשם `grades.txt`. כל שורה בקובץ מתעדת ציון של סטודנט בקורס מסוים, ומבנה השורה הוא: `ID,CourseName,Grade` (לדוגמה: `123456,Math,85`). סטודנט יכול להופיע במספר שורות שונות (עבור קורסים שונים או מועדים שונים באותו קורס).

כתוב פונקציה בשם `process_highest_grades(file_path)` המקבלת את שם הקובץ.

על הפונקציה לקרוא את הקובץ ולייצר **מילון** שבו המפתח הוא תעודת הזהות (מחרוזת), והערך הוא **Tuple** המכיל את `(CourseName, Grade)` של הקורס שבו קיבל הסטודנט את הציון _הגבוה ביותר_ שלו.

**דגשים ודרישות חובה:**

- אם יש מספר קורסים עם אותו ציון מקסימלי, ניתן לשמור כל אחד מהם.
    
- **טיפול בקבצים:** חובה להשתמש בבלוק `with open(...)` לקריאה בטוחה של הקובץ. עליך להפריד את השורות לנתונים הרלוונטיים (מותר להשתמש ב-`.split(',')`).
    
- **איסור "קסמים":** אין לאגור את כל הציונים של הסטודנט ואז להפעיל עליהם `max()`. יש לעדכן את המקסימום "על הדרך" תוך כדי קריאת השורות.
    
- **מקרי קצה:** אם הקובץ ריק, על הפונקציה להחזיר מילון ריק `{}`.
    

---

### שאלה 4: אופטימיזציה "הטריק הקלאסי" מ-$O(N^2)$ ל-$O(N)$

נתונה רשימה של מספרים שלמים וחיוביים `arr`, ונתון מספר שלם וחיובי `K`.

עליך למצוא האם קיימים ברשימה שני מספרים שההפרש המוחלט ביניהם הוא בדיוק `K` (כלומר, $|x - y| = K$).

כתוב פונקציה בשם `has_difference_k(arr, k)` המחזירה `True` אם קיים זוג כזה, ו-`False` אחרת.

**דגשים ודרישות חובה:**

- **אופטימיזציה (קריטי):** סטודנטים רבים פותרים שאלה זו בבחינה בעזרת שתי לולאות מקוננות שבודקות כל זוג אפשרי. פתרון זה יקבל 0 נקודות על סעיף היעילות. עליך לפתור את השאלה במעבר **אחד** בלבד על הרשימה ($O(N)$) תוך שימוש במבנה נתונים מסוג `Set`.
    
- מותר להניח ש-`K` תמיד גדול או שווה ל-0.
    
- **מקרי קצה:** אם הרשימה מכילה פחות מ-2 איברים, לא ייתכן זוג כזה ולכן יש להחזיר `False`. התייחס גם למקרה שבו $K=0$ (שאז נחפש האם יש מספר שמופיע יותר מפעם אחת).

---

### שאלה 5: הרכבת מבני נתונים - מילון של קבוצות

כתוב פונקציה בשם `map_failed_students(data_list)` המקבלת כקלט רשימה של טאפלים. כל טאפל מייצג רשומת בחינה ומכיל `(ID, Grade)` (לדוגמה: `("31234", 45)`).

על הפונקציה לבנות ולהחזיר מילון הממפה ציונים נכשלים לסטודנטים שקיבלו אותם.

המפתח במילון יהיה הציון (מספר שלם), והערך יהיה **Set** (קבוצה) המכיל את כל מספרי הזהות של הסטודנטים שקיבלו בדיוק את הציון הזה.

**דגשים ודרישות חובה:**

- **סינון:** יש להכניס למילון **אך ורק** ציונים נכשלים (ציון הקטן מ-55). ציונים של 55 ומעלה יש להתעלם מהם לחלוטין.
    
- אם סטודנט מופיע ברשימה פעמיים עם אותו ציון נכשל (למשל נכשל בשני קורסים שונים עם ציון 40), ה-Set צריך לדאוג באופן טבעי שהוא יופיע רק פעם אחת בתוך הקבוצה של אותו ציון.
    
- **מקרי קצה:** אם הרשימה ריקה, או שאין בה אף ציון נכשל, על הפונקציה להחזיר מילון ריק `{}`.
    
- עליך להקפיד על בניית המילון והקבוצות בצורה דינמית תוך כדי ריצה (בדיקה האם המפתח כבר קיים במילון לפני הוספה ל-Set שלו).

### שאלה 6
אתה מקבל str, עליך למנות כמה תווים יש מכל אחד, a = 2 b = 4 etc, תדפיס את זה ממוין: aabbbb לפי הגודל של כל אחד.
