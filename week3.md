
### שאלה 1: הפרדת זוגיים ואי-זוגיים (Two Pointers In-Place)

כתוב פעולה בשם `separate_evens_odds(arr)` המקבלת מערך (רשימה) של מספרים שלמים.

על הפעולה לסדר מחדש את איברי המערך כך שכל המספרים הזוגיים יופיעו בצד השמאלי של המערך, וכל המספרים האי-זוגיים יופיעו בצד הימני.

- **דגש יעילות (חובה לניקוד מלא):** עליך לבצע את המיון **במקום (In-Place)**, כלומר ללא יצירת מערך עזר חדש, ובמעבר אחד בלבד על המערך (סיבוכיות זמן $O(N)$ וסיבוכיות מקום $O(1)$). הסדר הפנימי של הזוגיים והאי-זוגיים בינם לבין עצמם אינו משנה.
    
- **מקרי קצה:** עליך להתייחס למצב שבו המערך ריק, או שכולו מכיל רק זוגיים/אי-זוגיים.

### שאלה 2: "מערך משלים" (זיהוי תבנית מ-2 הקצוות)

כתוב פעולה בשם `is_complementary_array(arr, K)` המקבלת מערך של מספרים שלמים ומספר שלם `K`.

המערך ייקרא "מערך משלים" אם סכום כל זוג איברים הנמצאים במרחק שווה משני קצוות המערך שווה בדיוק ל-`K`.

(כלומר, האיבר הראשון והאחרון מסתכמים ל-`K`, האיבר השני והלפני-אחרון מסתכמים ל-`K`, וכן הלאה).

- במקרה שבו אורך המערך אי-זוגי, האיבר האמצעי חייב להיות שווה בדיוק למחצית מ-`K` (כלומר `arr[mid] * 2 == K`).
    
- הפעולה תחזיר `True` אם המערך עונה על התנאי, ו-`False` אחרת.
    
- הפעלת פונקציה זו חייבת להיות יעילה, ולעצור מוקדם (Early Exit) ברגע שנמצא זוג שאינו מקיים את התנאי. אם המערך ריק, הפעולה תחזיר `True`.

### שאלה 3: חיפוש זוג בסכום נתון (במערך ממוין)

נתון מערך **ממוין בסדר עולה** של מספרים שלמים, ומספר שלם `S`.

כתוב פעולה בשם `find_pair_sum(arr, S)` הבודקת האם קיימים במערך שני איברים **שונים** שסכומם שווה ל-`S`.

- אם קיים זוג כזה, הפעולה תחזיר את שני המספרים כ-Tuple (למשל `(3, 7)`).
    
- אם לא קיים זוג כזה, הפעולה תחזיר `None`.
    
- **יעילות חובה:** מכיוון שהמערך ממוין, עליך להשתמש בטכניקת "שני מצביעים" כדי למצוא את הזוג בסיבוכיות זמן של $O(N)$. פתרון של לולאה מקוננת (אחת בתוך השנייה, $O(N^2)$) לא יתקבל ויגרור הורדת נקודות משמעותית.
    
### שאלה 4: סביבת אינדקס - פסגות מקומיות (Local Peaks)

כתוב פעולה בשם `count_local_peaks(arr)` המקבלת מערך של מספרים שלמים.

איבר במערך נחשב ל"פסגה מקומית" אם הוא **גדול ממש** משני השכנים שלו (האיבר שלפניו והאיבר שאחריו).

- עבור האיבר הראשון במערך (אינדקס 0), עליו להיות גדול ממש רק מהאיבר שאחריו.
    
- עבור האיבר האחרון במערך, עליו להיות גדול ממש רק מהאיבר שלפניו.
    
- הפעולה תספור כמה פסגות מקומיות יש במערך ותחזיר את הכמות.
    
- **דגש:** עליך להיזהר מחריגה מגבולות המערך (`IndexError`) בעת בדיקת השכנים של האיבר הראשון והאחרון! התמודד נכון עם מערכים באורך 0 או 1.

### שאלה 5: מעקב קוד (Tracing) ויעילות

לפניך קטע קוד של פעולה הכתובה בפייתון:

Python

```
def secret(arr):
    if len(arr) == 0:
        return 0
        
    count = 1
    i = 1
    j = 0
    
    while i < len(arr):
        if arr[i] != arr[j]:
            count += 1
            j = i
        i += 1
        
    return count
```

**ענה על הסעיפים הבאים (מומלץ להשתמש בטבלת מעקב בטיוטה שלך):**

א. עקוב בעזרת טבלת מעקב אחר זימון הפעולה: `secret([4, 4, 4, 7, 7, 2, 2, 2, 2, 8])`. מהו הערך שיוחזר?

ב. עקוב (ללא טבלת מעקב) אחר הזימון: `secret([1, 2, 3, 4, 5])`. מה יוחזר?

ג. נסח במדויק (במשפט אחד או שניים) מה מבצעת הפעולה `secret` באופן כללי עבור כל מערך שמועבר אליה. מה המשמעות של המספר המוחזר?

ד. מהי סיבוכיות הזמן (Time Complexity) של הפעולה? נמק בקצרה.
