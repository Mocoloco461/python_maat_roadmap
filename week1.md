# gemini response



### שאלה 1: תכונות של מספרים ומקרי קצה
כתבו פעולה `is_special_number(num)` המקבלת מספר שלם. 
הפעולה תחזיר `True` אם המספר הוא "מספר מיוחד", ו-`False` אחרת. 
"מספר מיוחד" מוגדר כמספר שבו סכום הספרות הזוגיות שבו שווה בדיוק לסכום הספרות האי-זוגיות שבו.
**דגשים:**
* על הפעולה להתמודד נכון עם קלט של מספר שלילי (יש להתייחס אליו בערכו המוחלט) ועם הקלט `0`.
* חובה להשתמש בלולאת `while` ופעולות חשבון (`//`, `%`) בלבד. 

### שאלה 2: הרכבת מספרים מחדש
כתבו פעולה בשם `filter_even_digits(num)` המקבלת מספר שלם חיובי `num`. 
הפעולה תיצור ותחזיר מספר שלם חדש המורכב אך ורק מהספרות הזוגיות של `num`, תוך שמירה על הסדר המקורי של הספרות (משמאל לימין).
* אם המספר המקורי לא הכיל כלל ספרות זוגיות, הפעולה תחזיר `0`.
* **לדוגמה:** עבור הזימון `filter_even_digits(83742)` הפעולה תחזיר את המספר `842`.
* מומלץ לחשוב על בניית המספר החדש בעזרת משתנה עזר (כופל) המייצג את המיקום העשרוני.

### שאלה 3: מעקב קוד (Tracing) - קלאסיקת מה"ט
נתונה הפעולה הבאה:
```python
def secret(num):
    if num == 0:
        return 0
    res = 0
    c = 1
    while num > 0:
        d = num % 10
        if d % 2 != 0:
            res = res + (d * c)
            c = c * 10
        num = num // 10
    return res
```
א. בצעו מעקב מדויק (באמצעות טבלת מעקב) עבור הזימון `secret(74251)` ורשמו מהו הערך שיוחזר.
ב. מה מבצעת הפעולה `secret` באופן כללי עבור כל מספר שלם חיובי שמועבר אליה? נסחו תשובה מילולית מדויקת.

### שאלה 4: מציאת מינימום/מקסימום אלגוריתמי
כתבו פעולה `digit_range(num)` המקבלת מספר שלם חיובי. 
הפעולה תמצא ותחזיר את ההפרש שבין הספרה הגדולה ביותר במספר לבין הספרה הקטנה ביותר במספר.
**דגשים:**
* איסור מוחלט על שימוש בפונקציות המובנות `max()` או `min()`, וכן אסור להמיר את המספר למבנה נתונים.
* עליכם לטפל במקרה קצה שבו המספר מורכב מ-0 או מספרה אחת בלבד (מה אמור להיות ההפרש במקרה כזה?).

### שאלה 5: שילוב אלגוריתמיקה של שלמים עם מערכים
כתבו פעולה `count_digit_match(arr, digit)` המקבלת מערך (רשימה) של מספרים שלמים `arr`, וספרה בודדת `digit` (בין 0 ל-9). 
הפעולה תחזיר כמה מספרים בתוך המערך מכילים את הספרה `digit` לפחות פעם אחת.
**דגשים:**
* הניחו שחלק מהמספרים במערך עלולים להיות שליליים.
* אם המערך ריק, הפעולה צריכה להחזיר `0`.
* מהי סיבוכיות זמן הריצה של הפעולה שכתבתם (כפונקציה של $N$ גודל המערך, ו-$K$ מספר הספרות המקסימלי במספר)?

### שאלה 6: לוגיקה עוקבת (מספר עולה-יורד)
מספר ייקרא "מספר הר" (Mountain Number) אם הספרות שלו עולות ממשמאל לימין עד לנקודת שיא מסוימת (שאינה הקצה הראשון או האחרון), ומאותה נקודה הספרות יורדות ממש עד סוף המספר.
לדוגמה: `13542` הוא מספר הר. `1234` אינו הר (רק עולה), `543` אינו הר (רק יורד).
כתבו פעולה `is_mountain(num)` המקבלת מספר שלם `num` (בן 3 ספרות לפחות) ומחזירה `True` אם הוא מספר הר, ו-`False` אחרת. 
*רמז: ניתן לחלק את הלולאות לחלק שמטפל בעלייה ולחלק שמטפל בירידה, אך עשו זאת בזהירות כדי לא "לאבד" מספרים קצרים.*

### שאלה 7: חיתוך מספרים
כתבו פעולה המקבלת שני מספרים שלמים חיוביים, `num1` ו- `num2`. 
הפעולה תחזיר `True` אם שתי הספרות הימניות ביותר של `num1` שוות – אך לא בהכרח באותו הסדר – לשתי הספרות הימניות ביותר של `num2`, אחרת תחזיר `False`. 
**דגשים:**
* הניחו שלכל מספר יש לפחות שתי ספרות.
* **לדוגמה:** עבור הזימונים עם `1459` ו- `3895` יוחזר `True` (שניהם מסתיימים בספרות 5 ו-9). עבור `12` ו-`21` יוחזר `True`.

### שאלה 8: יעילות זמן ריצה ומערכי מונים (Anagram Numbers)
כתבו פעולה `is_anagram_numbers(num1, num2)` המקבלת שני מספרים שלמים וחיוביים. 
הפעולה תבדוק האם שני המספרים מורכבים בדיוק מאותן ספרות ובאותה תדירות מופעים (כלומר, האם המספרים הם "אנאגרמה" מתמטית אחד של השני).
**דגשים ליעילות (קריטי):** כדי לקבל את מלוא הנקודות במבחן מה"ט על שאלה זו, עליכם להשתמש ב**מערך מונים** (תדירויות) בגודל 10 בלבד, ולרוץ על כל מספר פעם אחת. חל איסור למיין את הספרות או להשתמש בלולאה מקוננת (סיבוכיות ריבועית).

### שאלה 9: מעקב קוד - לולאות מתמטיות כפולות
נתונה הפעולה הבאה:
```python
def what(num):
    temp = num
    count = 0
    while temp > 0:
        count += 1
        temp //= 10

    res = 0
    temp2 = num
    while temp2 > 0:
        res += (temp2 % 10) ** count
        temp2 //= 10
        
    return res == num
```
א. עקבו בעזרת טבלת מעקב אחרי הזימון `what(153)` ורשמו איזה ערך בוליאני יוחזר.
ב. עקבו (ללא טבלה) אחרי הזימון `what(14)` ורשמו איזה ערך יוחזר.
ג. מה מייחד את המספרים שעבורם הפונקציה תחזיר `True`? נסחו במילים מה בודקת הפעולה ואיך משתנה ה-`count` משפיע על כך.

| num | temp | count | res | temp2 |
| --- | ---- | ----- | --- | ----- |
| 153 | 153  | 0     | -   | -     |
|     | 15   | 1     | -   | -     |
|     | 1    | 2     |     |       |
|     | 0    | 3     |     |       |
|     |      |       | 0   | 153   |
|     |      |       | 27  | 15    |
|     |      |       | 152 | 1     |
| 63  |      |       | 153 | 0     |







### שאלה 10: שילוב מורכב בין שני מספרים
כתבו פעולה `merge_numbers(num1, num2)` המקבלת שני מספרים שלמים חיוביים. **הניחו ששני המספרים הם בעלי אותו מספר של ספרות בדיוק.**
הפעולה תיצור ותחזיר מספר שלם חדש המורכב משילוב הספרות של שני המספרים לסירוגין, החל מספרת האחדות של `num1` ו-`num2` ועד לספרה השמאלית ביותר.
**לדוגמה:** עבור `num1 = 135` ו-`num2 = 246`:
* לוקחים את האחדות: 5 ואז 6
* לוקחים את העשרות: 3 ואז 4
* לוקחים את המאות: 1 ואז 2
המספר שיווצר ויוחזר יהיה: `123456`.
*חובה להרכיב את המספר מתמטית תוך שימוש במקדמים הנכונים (1, 10, 100...).*

---
**סיימת את הקוד? רוצה שאעשה לו Code Review ואבדוק אם הוא תואם לדרישות מבחן מה"ט (יעילות, טיפול באפסים וקוד אקדמי נקי)? העתק לכאן את הפתרונות שלך!**
