### פרומפט לייצירת תרגילים ושאלות:


"אני מתחיל עכשיו את הלמידה של **[]**.

אנא חזור לתוכנית הלימודים שבנינו עבורי, קרא את ה'נושא המרכזי' וה'תת-נושאים' של שבוע זה, ובדוק מהו ה'יעד השבועי' (כמה שאלות או איזו משימה עליי לבצע). כמו כן, שלוף מזיכרונך את 5 מבחני מה"ט בפייתון שניתחנו קודם לכן.

**המשימה שלך:** חבר לי בדיוק את כמות שאלות התרגול המוגדרת ב'יעד השבועי' של שבוע זה. השאלות חייבות להיות מנוסחות בדיוק בסגנון, ברמה ובפורמט של שאלות המבחן של מה"ט מתוך המבחנים שבזיכרון שלך.

**דגשים חמורים ליישום:**

1. **אל תכתוב לי את הפתרון!** הצג לי אך ורק את השאלות.
    
2. **מורכבות מבחן:** השאלות צריכות לדרוש כתיבת פונקציות, לכלול מקרי קצה (כגון רשימה ריקה, קלט שלילי/אפס, התמודדות עם נתונים חסרים), ולדרוש התייחסות לסיבוכיות ויעילות זמן ריצה.
    
3. **פייתון אקדמי ('ללא קסמים'):** נסח את השאלות כך שאצטרך לממש את הלוגיקה מאפס, ללא שימוש בפונקציות מובנות של פייתון (כמו `sum()`, `max()`, `min()`, `sort()` וכד') אלא אם הנושא דורש זאת במפורש.
    
4. **פרקטיקה בלבד:** ללא שאלות תיאורטיות. רק שאלות של 'כתוב פונקציה שמקבלת X, מבצעת Y ומחזירה Z', או שאלות מעקב קוד (Tracing) במידה וזה הנושא השבועי.



---
### חודש 1: אלגוריתמיקה קלאסית ושליטה במבני נתונים בסיסיים

**[[✅ שבוע 1 (12.4 - 18.4)]] | מניפולציות מתמטיות וספרות**
* **נושא מרכזי:** אלגוריתמיקה על שלמים (ללא המרה למחרוזת - טעות נפוצה שגוררת ירידה בנקודות על סיבוכיות מקום).
* **תת-נושאים ותבניות:** * תבנית ה-`while num > 0` יחד עם חילוק בשלם (`// 10`) ומודולו (`% 10`).
  * הרכבת מספרים חדשים מספרות, מציאת מינימום/מקסימום של ספרות, אלגוריתמים לבדיקת תכונות מספר ("מספר מושלם").
* **יעד שבועי:** לפתור 10 שאלות מבחן שלמות בנושאי מתמטיקה/ספרות, כתיבת קוד נקי ללא באגים של קצה (כמו התמודדות עם המספר 0).

[[✅ שבוע 2 (19.4 - 25.4)]] | מחרוזות ועיבוד טקסט (Validation)**
* **נושא מרכזי:** בדיקות תקינות על מחרוזות (מחרוזות חוקיות / סיסמאות).
* **תת-נושאים ותבניות:**
  * תבנית "הדגל" לאימות נתונים: סריקת מחרוזת, חיפוש הפרה של חוק, והחזרת `False` מוקדם (Early exit).
  * עבודה עם אינדקסים זוגיים/אי-זוגיים (`range(0, len(s), 2)`).
  * שימוש במתודות מחרוזת מוכרות כמו `find()`, `isupper()`, `isdigit()`.
* **יעד שבועי:** שליטה מוחלטת בבדיקות ולידציה. היעד הוא לכתוב פונקציות אימות יעילות במינימום שורות קוד.

[[✅ שבוע 3 (26.4 - 2.5)]] | מערכים, חיפושים ושני מצביעים**
* **נושא מרכזי:** מערכים חד-ממדיים ואלגוריתמי מעבר מתקדמים.
* **תת-נושאים ותבניות:**
  * טכניקת שני המצביעים (Two-Pointers): אינדקס מחוץ ללולאה (`start = 0`, `end = len(arr) - 1`), שמקודמים/נסוגים לפי תנאי. מעולה להפרדת זוגיים/אי-זוגיים, חיוביים/שליליים, ובדיקת פלינדרומים.
  * שימוש בסביבת אינדקס: בדיקת איבר ביחס לשכנים שלו `arr[i] vs arr[i-1]`.
* **יעד שבועי:** יישום טכניקת שני המצביעים ב-5 בעיות שונות, תוך הימנעות משגיאות `IndexError`.

**[[✅ שבוע 3.5 (3.5 - 9.5)]] | ארגז הכלים: מילונים, קבוצות, טאפלים וקריאת קבצים**
נושא מרכזי: שימוש במבני נתונים מתקדמים לפתרון בעיות ביעילות (O(1)).  
תת-נושאים ותבניות:
- מילונים (Dictionaries): מיפוי ערכים לערכים (Key-Value), מניית תדירויות (Frequency Map), וחיפוש ב-O(1).
- קבוצות (Sets) וטאפלים (Tuples): שימוש ב-Set לבדיקת קיום מהירה (Membership testing) ומניעת כפילויות; שימוש ב-Tuple כשלישיית/זוג ערכים לקיבוע נתונים.
- קריאת קבצים (File I/O): עבודה עם `with open(...)`, קריאת שורות/נתונים והמרתם לטיפוסי הנתונים שלמדת (למשל, הפיכת שורות בקובץ למילון של אובייקטים).
- אופטימיזציה: החלפת לולאה מקוננת (O(n^2)) בשימוש במילון (O(n)) – "טריק" קלאסי בשאלות מה"ט.  
    יעד שבועי: לפתור 5 בעיות אלגוריתמיות שונות: אחת המשתמשת במילון לתדירויות, אחת המשתמשת ב-Set, ואחת שדורשת קריאת קובץ ועיבוד הנתונים למבנה נתונים מסודר.

**[[שבוע 4 (3.5 - 9.5)]] | קריאת קוד, מעקב וניתוח עיוור**
* **נושא מרכזי:** שאלות מעקב (Tracing) - "מה הפעולה מבצעת?".
* **תת-נושאים ותבניות:**
  * בניית טבלאות מעקב מדויקות ויעילות (רישום כל משתנה מקומי, מעקב אחר לולאות מקוננות).
  * זיהוי מטרת האלגוריתם ממבט על (Top-Down comprehension) במקום ללכת לאיבוד בפרטים הקטנים.
* **יעד שבועי:** הגעה ל-100% דיוק במעקבים מבלי להריץ שורת קוד במחשב. (פותרים על נייר בלבד!).

---

### חודש 2: מבנים מורכבים ותכנות מונחה עצמים (OOP)

[[✅ שבוע 5 (10.5 - 16.5)]] | OOP בסיסי - בניית מחלקות**
* **נושא מרכזי:** כתיבת מחלקות (Classes) וניהול State.
* **תת-נושאים ותבניות:**
  * הגדרת הבנאי (`__init__`), שימוש נכון ב-`self`.
  * כתיבת מתודות פנימיות שמבצעות חישובים על תכונות המחלקה (למשל, חישוב הפרשי זמנים או עדכון סטטוס אובייקט).
  * מתודות השוואה המקבלות אובייקט נוסף מאותו סוג (כגון `def is_better(self, other):`).
* **יעד שבועי:** כתיבת מחלקות פשוטות ונטולות שגיאות בפחות מ-10 דקות (כדי לשמור זמן במבחן לחלקים הקשים).

**[[✅ שבוע 6 (17.5 - 23.5)]] | OOP מתקדם - מערכים של אובייקטים**
* **נושא מרכזי:** סריקה ועיבוד נתונים מתוך רשימות המכילות אובייקטים.
* **תת-נושאים ותבניות:**
  * לולאה שרצה על `list_of_objects`.
  * שילוב של מיומנויות מערך (שבוע 3) על רשימות עצמים: מציאת אובייקט עם ערך מקסימלי, ספירת אובייקטים המקיימים תנאי, וקריאה למתודות הפנימיות של כל אובייקט בתוך הלולאה.
* **יעד שבועי:** לפתור 5 שאלות OOP משולבות (חלק א': כתיבת המחלקה. חלק ב': כתיבת פונקציה חיצונית המקבלת מערך של מחלקות אלו).

**שבוע 7 (24.5 - 30.5) | רקורסיה**
* **נושא מרכזי:** מעקב קוד חכם אחרי פונקציות רקורסיביות והבנתן.
* **תת-נושאים ותבניות:**
  * זיהוי "תנאי עצירה" (Base Case) ו"צעד רקורסיבי".
  * הבנה האם הפונקציה "צוברת" ערך (כמו סכום), או מפלטרת (מחזירה True/False).
  * מעקב באמצעות "עץ קריאות" לעומת טבלה.
* **יעד שבועי:** לפצח את מטרתן של 10 פונקציות רקורסיביות ממבחני עבר תוך ציור עץ הקריאות שלהן במינימום זמן.

**שבוע 8 (31.5 - 6.6) | מערכים מקוננים וסיבוכיות (Big O)**
* **נושא מרכזי:** מטריצות (רשימת רשימות) וניתוח יעילות זמן הריצה.
* **תת-נושאים ותבניות:**
  * סריקה כפולה (שורות ועמודות).
  * שאלות יעילות: הבנה מתי קוד הוא $O(n)$, $O(n^2)$ או $O(\log n)$, ואיך לייעל פתרון (למשל, שימוש במערך תדירויות / מונים במקום לולאה בתוך לולאה).
* **יעד שבועי:** כתיבת פתרון שעובד, ואז אופטימיזציה שלו עד שנגיע לסיבוכיות הנדרשת בשאלות המה"ט.

---


> [!WARNING]
> **סימולציות:** בסימולציות (חודש 3), אל תתמקד רק בציון, אלא ב"זמן הליכה" – הקדש זמן לבדיקה ידנית (Dry Run) של כל קוד שכתבת, במיוחד עבור מקרי קצה (Edge Cases) כמו רשימות ריקות או ערכים לא תקינים.

### חודש 3: המאני טיים - סימולציות ומדידת זמנים (7.6 - 5.7)

*הנחיה לשבועות אלו: כל בחינה מתבצעת על טופס מלא, ללא עזרה חיצונית, עם סטופר (מומלץ להתחיל מ-3.5 שעות ולצמצם ל-3 שעות).*

**שבוע 9 (7.6 - 13.6) | סימולציות שלב א' - בניית סיבולת**
* **משימה:** ביצוע 2 מבחני עבר מלאים (למשל קיץ 2023 מועד א' וב').
* **יעד:** עמידה במסגרת הזמן. בחינה מדוקדקת של טעויות נגררות. אם חסרות נקודות בגלל "פינות" (Edge cases) – ליישם תיקונים מיידית.

**שבוע 10 (14.6 - 20.6) | סימולציות שלב ב' - אופטימיזציה של הציון**
* **משימה:** ביצוע 2 מבחנים מלאים נוספים (אביב 2024 וכו').
* **יעד:** ציון של 85+ בכל סימולציה. מיקוד בקבלת ניקוד מלא על שאלות ה-OOP שהן "מתנה" מבחינת יחס זמן/ניקוד, ועל טבלאות המעקב.

**שבוע 11 (21.6 - 27.6) | מקצה שיפורים וחידוד אחרון**
* **משימה:** חזרה ממוקדת אך ורק על השאלות שטעית בהן ב-4 הסימולציות הקודמות.
* **יעד:** הבנה של שורש הבעיה בטעויות (סיבוכיות? אינדקסים מחוץ למערך? בעיה בהבנת הרקורסיה?). 

**שבוע 12 (28.6 - 5.7) | סימולציית הגמר**
* **משימה:** מבחן מסכם אחד אחרון (רצוי השנה האחרונה ביותר שיש בנמצא) באווירה סטרילית. 
* **יעד:** סיום הבחינה 20 דקות לפני הזמן לטובת בדיקה (Dry run כפול) של הקוד שכתבת על נייר.

