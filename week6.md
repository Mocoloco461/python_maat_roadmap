


### שאלה 1: ניהול ספריות (מציאת מקסימום מותנה)

**חלק א':**

כתבו מחלקה בשם `Book` המייצגת ספר בספרייה.

1. כתבו פעולה בונה (`__init__`) המקבלת את התכונות הבאות:
    
    - `book_id`: מספר שלם המייצג את הקוד המזהה של הספר.
        
    - `borrow_count`: מספר שלם המייצג כמה פעמים הושאל הספר בעבר (ברירת מחדל: 0).
        
    - `is_damaged`: משתנה בוליאני המייצג האם הספר פגום (ברירת מחדל: `False`).
        
2. כתבו מתודה בשם `return_book(damaged_status)` המקבלת ערך בוליאני. המתודה תקדם את מספר ההשאלות (`borrow_count`) ב-1, ואם הערך שהתקבל הוא `True`, היא תעדכן את הסטטוס של הספר לפגום.
    

**חלק ב':**

כתבו פעולה **חיצונית** (פונקציה) בשם `get_most_popular_valid_book(books_arr)`. הפעולה מקבלת מערך של אובייקטים מסוג `Book`.

על הפעולה למצוא ולהחזיר את מזהה הספר (`book_id`) שהושאל הכי הרבה פעמים, **בתנאי שהוא אינו פגום**.

- **מקרי קצה חובה לטיפול:** אם המערך ריק, או שכל הספרים במערך פגומים, הפעולה תחזיר `-1`.
    
- **דגש יעילות ופייתון אקדמי:** חל איסור מוחלט להשתמש בפונקציות מובנות כגון `max()`, `sort()` או מיון המערך. עליכם לבצע את הסריקה במעבר יחיד ($O(N)$).
    

### שאלה 2: מכשירים בבית חכם (סריקה, עדכון ומונה)

**חלק א':**

כתבו מחלקה בשם `SmartDevice` המייצגת מכשיר חשמלי בבית חכם.

1. כתבו פעולה בונה המקבלת:
    
    - `device_name`: מחרוזת המייצגת את שם המכשיר.
        
    - `watts_per_hour`: מספר שלם וחיובי המייצג את צריכת החשמל של המכשיר לשעה.
        
    - `hours_active`: מספר שלם המייצג כמה שעות המכשיר פעל היום.
        
    - `is_on`: משתנה בוליאני המייצג האם המכשיר דולק כעת.
        
2. כתבו מתודה בשם `get_daily_consumption()` המחזירה את סך צריכת החשמל של המכשיר היום (מכפלת הצריכה לשעה במספר שעות הפעילות).
    
3. כתבו מתודה בשם `turn_off()` המכבה את המכשיר (משנה את `is_on` ל-`False`).
    

**חלק ב':**

כתבו פעולה **חיצונית** בשם `turn_off_heavy_consumers(devices_arr, limit_watts)`. הפעולה מקבלת מערך של אובייקטים מסוג `SmartDevice` וכן מספר שלם `limit_watts` המייצג את רף הצריכה המקסימלי המותר.

על הפעולה לסרוק את כל המכשירים. כל מכשיר שדולק כעת **וכן** צריכת החשמל היומית שלו עולה על `limit_watts`, יש לכבות אותו בעזרת המתודה המתאימה. הפעולה תחזיר כפלט את **כמות** המכשירים שכובו בפועל על ידה.

- **מקרי קצה:** במידה ו-`limit_watts` הוא מספר שלילי או 0, אין לכבות אף מכשיר והפעולה תחזיר 0.
    

### שאלה 3: מערכת חיישני חניון (שימוש במערך מונים / תדירויות)

**חלק א':**

כתבו מחלקה בשם `ParkingSensor` המייצגת חיישן בחניון.

1. כתבו פעולה בונה המקבלת:
    
    - `sensor_id`: מספר שלם מזהה.
        
    - `floor`: מספר שלם בין 1 ל-5 המייצג את הקומה בה החיישן מותקן (הניחו שהקלט תמיד תקין).
        
    - `cars_passed`: מספר שלם המייצג כמה רכבים חלפו על פני החיישן מתחילת היום.
        
    - `is_active`: בוליאני המייצג האם החיישן תקין ופעיל.
        

**חלק ב':**

כתבו פעולה **חיצונית** בשם `find_busiest_floor(sensors_arr)`. הפעולה מקבלת מערך של אובייקטים מסוג `ParkingSensor`.

על הפעולה לחשב מהי הקומה (מ-1 עד 5) שעברו בה הכי הרבה רכבים בסך הכל, **על סמך החיישנים הפעילים בלבד**. הפעולה תחזיר את מספר הקומה העמוסה ביותר.

- **דגש אלגוריתמי מרכזי:** מכיוון שידוע שיש בדיוק 5 קומות, עליכם להשתמש ב**מערך מונים (Counters)** בגודל מתאים כדי לאגור את כמות הרכבים לכל קומה, ורק לאחר מכן למצוא את המקסימום. שימוש בלולאות מקוננות ($O(N^2)$) יגרור הורדת נקודות.
    
- **מקרי קצה:** אם המערך הנתון ריק, יש להחזיר 0.
     ⏳[4ec931bd|stopwatch|paused|519|1780303485|0]

### שאלה 4: עיבוד נתוני עובדים ותלושי שכר (חישוב מורכב ולוגיקה)

**חלק א':**

כתבו מחלקה בשם `Employee` המייצגת עובד בחברה.

1. כתבו פעולה בונה המקבלת:
    
    - `emp_id`: מחרוזת מזהה.
        
    - `base_salary`: מספר שלם חיובי.
        
    - `sales_amount`: מספר שלם המייצג את סכום המכירות שביצע העובד החודש.
        
2. כתבו מתודה בשם `calculate_bonus()` המחשבת ומחזירה את בונוס המכירות של העובד לפי החוקיות הבאה:
    
    - אם סכום המכירות קטן מ-5,000 ש"ח - אין בונוס (יוחזר 0).
        
    - אם סכום המכירות בין 5,000 ל-10,000 ש"ח (כולל) - הבונוס הוא 10% מסכום המכירות.
        
    - אם סכום המכירות מעל 10,000 ש"ח - הבונוס הוא 20% מסכום המכירות.
        
        _(הניחו שהפונקציה יכולה להחזיר מספר עשרוני)._
        
3. כתבו מתודה `get_total_salary()` המחזירה את השכר הכולל של העובד (שכר בסיס + בונוס).
    

**חלק ב':**

כתבו פעולה **חיצונית** בשם `get_average_salary(emp_arr)`. הפעולה מקבלת מערך של עובדים. הפעולה תחשב ותחזיר את ממוצע השכר הכולל (בסיס + בונוסים) של כל העובדים במערך.

- **דגש:** חל איסור להשתמש ב-`sum()` או `len()` במערך (אלא אם אתם משתמשים בלולאת `for i in range` הדורשת אורך, אך את ספירת העובדים עצמם יש לבצע ידנית אם תבחרו ב-`for obj in arr`).
    
- **מקרי קצה:** כיצד תמנעו משגיאת `ZeroDivisionError` אם מועבר מערך ריק? במקרה כזה, יש להחזיר 0.
    
 ⏳[c08ae4c1|stopwatch|paused|617|1780304923|0]
### שאלה 5: מעקב משלוחים (מניפולציה וסינון של רשימת אובייקטים)

**חלק א':**

כתבו מחלקה בשם `Delivery` המייצגת משלוח.

1. כתבו פעולה בונה המקבלת:
    
    - `tracking_number`: מחרוזת.
        
    - `weight`: מספר עשרוני המייצג משקל בק"ג.
        
    - `status`: מחרוזת היכולה להיות אחת מתוך שלוש אפשרויות: `"Pending"`, `"Shipped"`, `"Delivered"`.
        
2. כתבו מתודה בשם `update_status(new_status)`. המתודה מקבלת מחרוזת. אם המחרוזת שהתקבלה היא אחת משלושת הסטטוסים החוקיים המוזכרים למעלה, המתודה תעדכן את הסטטוס ותחזיר `True`. אם הסטטוס לא חוקי, הסטטוס לא ישתנה והמתודה תחזיר `False`.
    

**חלק ב':**

כתבו פעולה **חיצונית** בשם `extract_heavy_pending(deliveries_arr, min_weight)`. הפעולה מקבלת מערך של משלוחים ומספר עשרוני `min_weight`.

על הפעולה לסרוק את המערך ולייצר **מערך חדש** (רשימה חדשה) שיכיל רק את מספרי המעקב (`tracking_number`) של משלוחים העונים על **שני התנאים הבאים יחד**:

1. סטטוס המשלוח הוא `"Pending"`.
    
2. משקל המשלוח גדול ממש מה-`min_weight` שהתקבל.
    
    בסיום, הפעולה תחזיר את הרשימה החדשה שיצרה.
    

- **יעילות:** אין צורך לייצר אובייקטים חדשים, רק לאסוף את מחרוזות מספרי המעקב לתוך הרשימה בסיבוכיות של $O(N)$.
    
- **מקרי קצה:** אם אין אף משלוח שעונה על התנאים, על הפעולה להחזיר רשימה ריקה (`[]`).
 ⏳[feaad7ec|stopwatch|paused|804|1780306158|0]