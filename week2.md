### **שאלה 1: תבנית ה-"דגל" ויציאה מוקדמת (Early Exit)**
כתוב פעולה (פונקציה) בשם `is_strong_password(pwd)` המקבלת מחרוזת המייצגת סיסמה. 
הפעולה תחזיר `True` אם הסיסמה עומדת **בכל** התנאים הבאים, ו-`False` אחרת:
1. אורך הסיסמה הוא לפחות 8 תווים.
2. התו הראשון בסיסמה חייב להיות אות אנגלית גדולה (Uppercase).
3. הסיסמה אינה מכילה רווחים כלל.
4. הסיסמה מכילה **לפחות שתי ספרות** במקומות כלשהם במחרוזת.

**דגשים למימוש:**
* עליך להשתמש בלולאה אחת בלבד הסורקת את המחרוזת (סיבוכיות זמן של $O(n)$).
* יש לממש יציאה מוקדמת (`return False` או עצירת הלולאה עם דגל) ברגע שנמצאת הפרה של אחד מהחוקים (למשל, זיהוי רווח).
* התייחס למקרי קצה: מחרוזת ריקה, או מחרוזת קצרה מ-8 תווים.

---


### **שאלה 2: אינדקסים זוגיים ואי-זוגיים**
כתוב פעולה בשם `is_alternating_string(s)` המקבלת מחרוזת. 
מחרוזת of תיחשב "חוקית" אם היא עומדת בחוקיות הבאה:
1. המחרוזת מכילה אותיות אנגליות בלבד (ללא ספרות, רווחים או סימני פיסוק).
2. כל התווים באינדקסים הזוגיים (0, 2, 4...) הם אותיות אנגליות גדולות (`A-Z`).
3. כל התווים באינדקסים האי-זוגיים (1, 3, 5...) הם אותיות אנגליות קטנות (`a-z`).

**דגשים למימוש:**
* מותר (ומומלץ) להשתמש במתודות כמו `isupper()`, `islower()` ו-`isalpha()`.
* נסה לממש את הפתרון בעזרת קפיצות ב-`range` או בעזרת בדיקת מודולו על האינדקס הנוכחי.
* התייחס למקרה שבו מועברת מחרוזת ריקה (הפעולה צריכה להחזיר `True` כי אין בה הפרה של התנאים).
 ⏳[d0ce121f|stopwatch|stopped|1162|1776679293|0] - 1
  ⏳[f92d5be1|stopwatch|stopped|494|1776679291|0]


---

### **שאלה 3: חיתוך וזיהוי תבניות מתקדמות**
כתוב פעולה בשם `validate_serial_code(code)` המקבלת מחרוזת המייצגת מק"ט (מספר קטלוגי).
על המק"ט להיות במבנה המדויק הבא: `PREFIX-NUMBER-SUFFIX`
* **PREFIX**: בדיוק שתי אותיות אנגליות גדולות.
* **NUMBER**: רצף של בין 4 ל-6 ספרות (כולל).
* **SUFFIX**: בדיוק אות אנגלית קטנה אחת.
* בין כל חלק מפריד התו מקף (`-`).

**דגשים למימוש:**
* מותר להשתמש בפעולה `find()` כדי לאתר את המקפים ולחלץ את חלקי המחרוזת (Slicing).
* אין להשתמש בפעולות מתקדמות של פיצול כגון `split()`. עליך לבצע את החיתוך והבדיקה ידנית.
* מקרה קצה: ודא שהפעולה שלך לא קורסת (זורקת שגיאת `IndexError`) אם מועברת מחרוזת שאין בה מקפים כלל או שיש בה פחות/יותר מדי מקפים. הפעולה פשוט צריכה להחזיר `False`.

 ⏳[b1f411de|stopwatch|stopped|3132|1776679286|0]
	 ⏳[1115db6a|stopwatch|stopped|1252|1776679285|0]

---

### **שאלה 4: שאלת שלד (סינתזה של כל החוקים)**
כתוב פעולה בשם `is_valid_sentence(text)` המקבלת מחרוזת המייצגת משפט.
משפט מוגדר "תקין" אם הוא עומד **בכל** התנאים הבאים במעבר אחד בלבד (זמן ריצה $O(n)$):
1. המשפט מתחיל באות אנגלית גדולה.
2. המשפט מסתיים בתו נקודה (`.`).
3. המשפט אינו מכיל שני רווחים רצופים או יותר.
4. המשפט אינו מכיל ספרות כלל.

**דגשים למימוש:**
* חובה לממש את הבדיקה בעזרת סריקה אחת של המחרוזת.
* אין להשתמש בפעולה `replace()` כדי לבדוק רווחים כפולים. עליך לזהות אותם תוך כדי המעבר בלולאה (רמז: שמירת התו הקודם, או בדיקת `text[i]` מול `text[i+1]`).
* גם כאן, זכור לממש יציאה מוקדמת כדי לחסוך זמן ריצה ברגע שאחד התנאים מופר.
 ⏳[c8cb5df9|stopwatch|paused|931|1777188753|0]




